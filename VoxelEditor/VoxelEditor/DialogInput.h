#pragma once
#include "resource.h"
#include "GLTools.h"

BOOL CALLBACK dialogInputDeviceProc(HWND h, UINT m, WPARAM w, LPARAM l);
BOOL CALLBACK dialogInputTextProc(HWND h, UINT m, WPARAM w, LPARAM l);
